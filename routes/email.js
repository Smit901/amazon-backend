const express = require('express')
// const { orderConfirmation, shippingUpdates, resetPassword, resetPasswordConfirmation } = require('../controllers/email')
// const asyncRouteHandler = require('../util/asyncRouteHandler')

const router = express.Router()

// dev only
// router.post('/orderConfirmation', asyncRouteHandler(orderConfirmation))
// router.post('/shippingUpdates', asyncRouteHandler(shippingUpdates))
// router.post('/resetPassword', asyncRouteHandler(resetPassword))
// router.post('/resetPasswordConfirmation', asyncRouteHandler(resetPasswordConfirmation))

module.exports = router
